@using MessengerNetSix.Views.Shared
@model IEnumerable<MessengerNetSix.Models.ChatMember>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@*<component type="typeof(Counter)" render-mode="ServerPrerendered" />*@
@*<component type="typeof(Chat)" render-mode="ServerPrerendered" reciever="Bonga"/>*@
@{
    if (ViewBag.Key!=null){
@(await Html.RenderComponentAsync<Chat>(RenderMode.ServerPrerendered, new { Sender=User.Identity.Name, Connection=ViewBag.ChatId, DisplayName=ViewBag.ChatName, messages=ViewBag.messages, Key = ViewBag.Key}))
}
else{
    @(await Html.RenderComponentAsync<Chat>(RenderMode.ServerPrerendered, new { Sender=User.Identity.Name, Connection=ViewBag.ChatId, DisplayName=ViewBag.ChatName, messages=ViewBag.messages}))

}
}

<script type="text/javascript">
 /* window.onload=function(){
    setTimeout(5000);
    var block = document.getElementById("round");
    block.scrollTop = 9999;
  }*/
  //round.scrollTop = round.scrollHeight;
</script>